function ocultarF() {
    var styles = window.getComputedStyle(
        document.getElementsByClassName('bc1'),
        ':before'
      )
      console.log(styles);
    // const verm = document.getElementById('bConferir').getElementsByClassName('bc1');

    // verm.dataset.content = 'Muda valor da propriedade, e isso reflete no pseudo-elemento';
    
    
}